import React from 'react';

const userOutput = (props) => {
    return(
        <div>
            <p>{props.username}</p>
            <p>Test paragraph 2</p>
        </div>
    );
}

export default userOutput;